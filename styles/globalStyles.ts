import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
  container: {
    flex: 1,
    gap: 20,
  },

  center: {
    alignItems: "center",
    justifyContent: "center",
  },

  input: {
    height: 50,
    width: 300,
    borderRadius: 10,
    borderWidth: 1,
    borderColor: "#c0d9b8",
    padding: 15,
    color: "#1f2f18",
  },

  // mainColor: {
  //   color: "#adb4aa",
  // },
});
